node_modules
.next

# local env files (secrets — never commit)
.env*.local
.env.local

# Design system (work in progress, not ready for repo)
docs/design-system/

# local lesson authoring materials (pptx/docx, not for repo)
lesson_4/

# build / tooling artifacts
.test-dist/
tsconfig.tsbuildinfo

# local Claude Code config
.claude/

# local Codex configs with credentials
.codex/*.local.toml

# deprecated local V1 operations material with historical plaintext secrets
enviromnent/

# local recovery snapshots (database dumps, repository bundles, secrets)
.local-backups/
